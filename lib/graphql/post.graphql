mutation deletePost($id: Int!) {
  deletePost(id: $id)
}

mutation createPost(
  $description: String
  $hashtags: String
  $photoDate: String!
  $latitude: Float!
  $longitude: Float!
  $photo: Upload!
) {
  createPost(
    input: {
      description: $description
      hashtags: $hashtags
      photoDate: $photoDate
      latitude: $latitude
      longitude: $longitude
      photo: $photo
    }
  )
}

query personalizedPosts {
  personalizedPosts {
    id
  }
}

query tag($label: String!) {
  tag(label: $label) {
    label
    posts {
      description
    }
  }
}

query post($id: Int!) {
  post(id: $id) {
    createdAt
    postDate
    description
    hashtags {
      name
    }
    url
    author {
      id
      username
      firstName
      lastName
    }
    likes {
      id
      username
      firstName
      lastName
    }
  }
}
