mutation delete($id: Int!) {
  delete(id: $id)
}

mutation createComment($target: Int!, $content: String!) {
  createComment(input: { target: $target, content: $content })
}

mutation createPost(
  $description: String
  $hashtags: String
  $photoDate: String!
  $latitude: Float!
  $longitude: Float!
  $photo: [Upload!]!
) {
  createPost(
    input: {
      description: $description
      hashtags: $hashtags
      photoDate: $photoDate
      latitude: $latitude
      longitude: $longitude
      photo: $photo
    }
  )
}

query personalizedPosts($skip: Int!, $take: Int!) {
  personalizedPosts(input: { skip: $skip, take: $take }) {
    id
    description
    author {
      id
      firstName
      lastName
      username
      profileUrl
    }
    url
    createdAt
    postDate
    likeCount
    commentCount
    liked
  }
}

query tag($label: String!) {
  tag(label: $label) {
    label
    posts {
      description
    }
  }
}

query post($id: Int!) {
  post(id: $id) {
    id
    liked
    createdAt
    postDate
    description
    hashtags {
      name
    }
    url
    author {
      id
      username
      firstName
      lastName
    }
    likes {
      id
      username
      firstName
      lastName
    }
    comments {
      id
      liked
      createdAt
      content
      author {
        id
        username
        firstName
        lastName
      }
    }
  }
}

query posts($input: PostsInput) {
  posts(input: $input) {
    id
    url
    postDate
    description
    createdAt
    author {
      id
      username
      firstName
      lastName
    }
    place {
      id
      name
      description
      latitude
      longitude
    }
  }
}
