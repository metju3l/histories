query Paths {
  paths {
    name
    coordinates
  }
}

query MapPosts(
  $maxLatitude: Float!
  $minLatitude: Float!
  $maxLongitude: Float!
  $minLongitude: Float!
) {
  mapPosts(
    input: {
      maxLatitude: $maxLatitude
      minLatitude: $minLatitude
      maxLongitude: $maxLongitude
      minLongitude: $minLongitude
    }
  ) {
    id
    latitude
    longitude
  }
}

query Place($id: Int!) {
  place(id: $id) {
    posts {
      id
    }
  }
}
