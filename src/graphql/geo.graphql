query Paths {
  paths {
    name
    coordinates
  }
}

query MapPosts(
  $maxLatitude: Float!
  $minLatitude: Float!
  $maxLongitude: Float!
  $minLongitude: Float!
) {
  mapPosts(
    input: {
      maxLatitude: $maxLatitude
      minLatitude: $minLatitude
      maxLongitude: $maxLongitude
      minLongitude: $minLongitude
    }
  ) {
    id
    latitude
    longitude
    posts {
      id
      url
      description
      author {
        id
        username
        firstName
        lastName
      }
    }
  }
}

query Place($id: Int!) {
  place(id: $id) {
    posts {
      id
    }
  }
}
