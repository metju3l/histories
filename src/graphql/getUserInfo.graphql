query getUserInfo($username: String!) {
  getUserInfo(input: { username: $username }) {
    username
    email
    firstName
    lastName
    bio
    authenticated
    created_at
    following {
      email
      username
      firstName
      lastName
    }
    followers {
      email
      username
      firstName
      lastName
    }
    collections {
      collectionName
      createdAt
    }
  }
}
