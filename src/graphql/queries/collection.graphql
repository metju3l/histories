query Collection($id: Int!) {
  collection(id: $id) {
    id
    postCount
    preview
    name
    description
    createdAt
    author {
      id
      username
      firstName
      lastName
      bio
      followerCount
      followingCount
      isFollowing
      profile
    }
    posts {
      id
    }
  }
}
